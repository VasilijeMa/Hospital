<Window x:Class="ZdravoCorp.GUI.View.Patient.PatientNotificationsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ZdravoCorp.GUI.View.Patient"
        mc:Ignorable="d"
        Title="PatientNotificationsView" Height="450" Width="800">
    <Grid>
        <DataGrid AutoGenerateColumns="True" ColumnWidth="155" RowHeight="20" ItemsSource="{Binding Notidications, Mode=TwoWay}" SelectedItem="{Binding Notification, Mode=TwoWay}" IsReadOnly="True" x:Name="dataGrid" Margin="0,8,0,145">
        </DataGrid>
    </Grid>
</Window>
